# Object.keys()

Object ထဲက keys တွေကို array အနေနဲ့ပြန်ပေးပါတယ်။

```js
const user = {
  name: "Mg Mg",
  age: 25
};

console.log(Object.keys(user));
```

## Output

```txt
[ 'name', 'age' ]
```

---