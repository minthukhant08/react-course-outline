# Object.entries()

Key–value pair တွေကို nested array ပုံစံနဲ့ပြန်ပေးပါတယ်။

```js
const user = {
  name: "Mg Mg",
  age: 25
};

console.log(Object.entries(user));
```

## Output

```txt
[
  [ 'name', 'Mg Mg' ],
  [ 'age', 25 ]
]
```

---