
# Deleting Properties

`delete` keyword ကိုသုံးပြီး property ဖျက်နိုင်ပါတယ်။

## Example
```js
const user = {
  name: "Mg Mg",
  age: 25
};

delete user.age;

console.log(user);
```

### Output

```txt
{ name: 'Mg Mg' }
```
