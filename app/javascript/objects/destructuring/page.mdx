# Destructuring

Object ထဲက values တွေကို variable အဖြစ် တိုက်ရိုက်ထုတ်ယူနိုင်ပါတယ်။

```js
const user = {
  name: "Mg Mg",
  age: 25
};

const { name, age } = user;

console.log(name);
console.log(age);
```

## Output

```txt
Mg Mg
25
```
