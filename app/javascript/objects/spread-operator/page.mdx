# Spread Operator

Object copy လုပ်ဖို့ spread operator ကိုသုံးနိုင်ပါတယ်။

```js
const user1 = {
  name: "Mg Mg",
  age: 25
};

const user2 = { ...user1 };

console.log(user2);
```

## Output

```txt
{ name: 'Mg Mg', age: 25 }
```
