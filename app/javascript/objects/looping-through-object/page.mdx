# Looping Through Objects

`for...in` loop နဲ့ object properties တွေကို loop ပတ်နိုင်ပါတယ်။

```js
const user = {
  name: "Mg Mg",
  age: 25
};

for (let key in user) {
  console.log(key, user[key]);
}
```

## Output

```txt
name Mg Mg
age 25
```
